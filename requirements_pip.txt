# From this repo root (with env activated):
#   bash install_pytorch.sh
#   pip install --pre --no-build-isolation -r requirements_pip.txt
pillow
rasterio
fastai
ipython
-r requirements_git_deps_shared.txt
psycopg2-binary
reportlab
huggingface_hub
-e .
